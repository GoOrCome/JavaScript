function calculateTotalCartValue(...total) {
    // let totalValue = 0;
    // console.log(total);
    return total;
    // for (let i = 0; i < arguments.length; i++) {
    //   totalValue += arguments[i];
    // }
    // console.log(`The total cart value is ${totalValue}`);
    // return totalValue;
  }
//   for (let index = 0; index < array.length; index++) {
//     const element = array[index];
    
//   }
let p = calculateTotalCartValue(125, 20, 30);
console.log(p);
function totalvalues(){
    let a=0;
    p.forEach(nub=> p+= nub);
}

// console.log(calculateTotalCartValue(125, 20, 30));
totalvalues(p);
  
  // OUTPUT: The total cart value is 175
  